---
import ToolLayout from '../layouts/ToolLayout.astro';
import QrCodeTool from '../components/tools/QrCodeTool.tsx';
---
<ToolLayout slug="qrcode">
  <QrCodeTool client:load />
  <div slot="seo-content">
    <h2>二维码生成与解析工具</h2>
    <p>在线生成二维码图片、解析二维码内容，支持自定义尺寸、容错等级和前景背景色。所有处理在浏览器本地完成，图片不上传服务器，适合包含敏感信息的二维码场景。</p>
    <h3>容错等级说明</h3>
    <ul>
      <li><strong>L（约 7%）</strong>：最低容错，码图最紧凑，适合干净环境。</li>
      <li><strong>M（约 15%）</strong>：默认等级，平衡容错与密度，多数场景适用。</li>
      <li><strong>Q（约 25%）</strong>：较高容错，可遮挡少量区域。</li>
      <li><strong>H（约 30%）</strong>：最高容错，适合印刷、带 logo、易污损场景。</li>
    </ul>
    <h3>常见使用场景</h3>
    <ul>
      <li>网址、文本、Wi-Fi 配置、名片（vCard）生成二维码。</li>
      <li>扫码登录回调 URL 测试、支付链接转码。</li>
      <li>识别并解析已有二维码图片内容，反向提取文本。</li>
    </ul>
    <h3>常见问题</h3>
    <div class="faq-item"><div class="faq-q">二维码最大能放多少内容？</div><div class="faq-a">取决于版本和容错等级：版本 40 在 L 级下最多约 2953 字节（纯数字 7089 个）。内容越多码图越密，建议长文本先用短链接服务压缩。</div></div>
    <div class="faq-item"><div class="faq-q">扫描识别不出来怎么办？</div><div class="faq-a">请确认图片清晰、对比度足够、未被裁剪掉边缘空白区域；H 级容错的码图即使污损 30% 仍可识别，必要时重新生成提高容错等级。</div></div>
    <div class="faq-item"><div class="faq-q">支持中文吗？</div><div class="faq-a">支持。本工具使用 UTF-8 编码，中文、Emoji 均可正常生成与解析。</div></div>
  </div>
</ToolLayout>
