---
import ToolLayout from '../layouts/ToolLayout.astro';
import CssFormatter from '../components/tools/CssFormatter.tsx';
---
<ToolLayout slug="css-formatter">
  <CssFormatter client:load />
  <div slot="seo-content">
    <h2>CSS 格式化与压缩工具</h2>
    <p>在线美化、压缩 CSS（含 SCSS/Less 语法糖）代码。美化基于 Prettier postcss 引擎，压缩模式去除注释、折叠空白、清除选择器与标点周围的冗余空格，紧凑且语义安全。</p>
    <h3>压缩效果示例</h3>
    <p>压缩前：</p>
    <pre><code>{'.btn { padding : 8px 16px ; color : #fff ; /* 按钮 */ }'}</code></pre>
    <p>压缩后：</p>
    <pre><code>{'.btn{padding:8px 16px;color:#fff}'}</code></pre>
  </div>
</ToolLayout>
