---
import ToolLayout from '../layouts/ToolLayout.astro';
import JsonFormatter from '../components/tools/JsonFormatter.tsx';

const slug = 'json-formatter';
---
<ToolLayout slug={slug}>
  <JsonFormatter client:load />

  <div slot="seo-content">
    <h2>JSON 格式化工具</h2>
    <p>
      JSON（JavaScript Object Notation）是一种轻量级的数据交换格式，是当前 Web 开发中最流行的数据传输格式。
      本工具提供 JSON 的<strong>在线格式化、压缩、校验</strong>功能，全部在浏览器本地完成，不上传任何数据。
    </p>

    <h3>功能说明</h3>
    <ul>
      <li><strong>格式化</strong>：将压缩成一行的 JSON 自动缩进换行，支持 2 空格 / 4 空格 / 制表符三种缩进方式。</li>
      <li><strong>压缩</strong>：移除所有空白字符，将 JSON 压缩成一行，减小传输体积。</li>
      <li><strong>校验</strong>：检查 JSON 语法是否正确，定位错误位置。</li>
      <li><strong>统计</strong>：实时显示键数量、层级深度、文件大小。</li>
    </ul>

    <h3>使用教程</h3>
    <ol>
      <li>在左侧输入框粘贴需要处理的 JSON 字符串。</li>
      <li>在工具栏选择模式（格式化 / 压缩 / 校验）和缩进方式。</li>
      <li>点击主按钮，右侧即时显示结果。</li>
      <li>点击「复制」将结果复制到剪贴板。</li>
    </ol>

  </div>
</ToolLayout>
