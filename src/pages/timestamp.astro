---
import ToolLayout from '../layouts/ToolLayout.astro';
import TimestampConverter from '../components/tools/TimestampConverter.tsx';
---
<ToolLayout slug="timestamp">
  <TimestampConverter client:load />
  <div slot="seo-content">
    <h2>Unix 时间戳转换工具</h2>
    <p>Unix 时间戳是从 1970-01-01 00:00:00 UTC 到指定时刻的秒数（或毫秒数）。本工具支持时间戳与日期互转，显示本地时间、UTC 时间、星期、相对时间。</p>
    <h3>常见用法</h3>
    <ul>
      <li>接口调试：后端返回时间戳，需转为可读时间。</li>
      <li>日志分析：将日志中的时间戳转为本地时间。</li>
      <li>数据库：datetime 与 timestamp 字段互转。</li>
    </ul>
    <h3>常见问题</h3>
    <div class="faq-item"><div class="faq-q">秒和毫秒时间戳怎么区分？</div><div class="faq-a">10 位数字通常是秒级（Unix 时间戳），13 位数字通常是毫秒级（JavaScript 默认）。本工具会自动识别。</div></div>
    <div class="faq-item"><div class="faq-q">时区是怎样的？</div><div class="faq-a">时间戳本身是 UTC 时区。转换结果会同时显示本地时间和 UTC 时间，便于跨时区对照。</div></div>
  </div>
</ToolLayout>
