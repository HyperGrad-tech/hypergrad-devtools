---
import ToolLayout from '../layouts/ToolLayout.astro';
import PdfMerge from '../components/tools/PdfMerge.tsx';
---
<ToolLayout slug="pdf-merge">
  <PdfMerge client:load />
  <div slot="seo-content">
    <h2>在线 PDF 合并工具</h2>
    <p>把多个 PDF 文件按顺序合并为一个 PDF。支持拖拽添加、调整顺序、本地预览与下载，全程在浏览器内处理，文件不离开电脑，适合合同、发票、报告等敏感文档。</p>
    <h3>使用步骤</h3>
    <ol>
      <li>点击或拖拽一个或多个 PDF 文件到上传区。</li>
      <li>使用 ↑ ↓ 按钮调整合并顺序，删除不需要的文件。</li>
      <li>点击「合并」按钮，生成后直接下载或预览。</li>
    </ol>
    <h3>常见问题</h3>
    <div class="faq-item"><div class="faq-q">合并后的 PDF 会被压缩吗？</div><div class="faq-a">不会。本工具做的是无损页面拼接，保留原 PDF 的页面、文字与图片质量。如需压缩请使用图片压缩工具把扫描页压小后再合并。</div></div>
    <div class="faq-item"><div class="faq-q">加密的 PDF 能合并吗？</div><div class="faq-a">需先解除密码保护的 PDF 才能合并。本工具不会破解加密文档，遇到加密文件时会提示加载失败。</div></div>
    <div class="faq-item"><div class="faq-q">文件会上传服务器吗？</div><div class="faq-a">不会。合并完全在浏览器使用 pdf-lib 库本地完成，断网也可使用，处理完不保留任何文件。</div></div>
  </div>
</ToolLayout>
