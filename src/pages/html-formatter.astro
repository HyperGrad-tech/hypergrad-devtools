---
import ToolLayout from '../layouts/ToolLayout.astro';
import HtmlFormatter from '../components/tools/HtmlFormatter.tsx';
---
<ToolLayout slug="html-formatter">
  <HtmlFormatter client:load />
  <div slot="seo-content">
    <h2>HTML 格式化与压缩工具</h2>
    <p>在线美化、压缩 HTML 代码。美化基于 Prettier html 引擎，正确处理嵌套标签缩进。压缩模式去除注释、折叠标签间空白，适合减小首页体积、提升页面加载速度。</p>
    <h3>压缩注意事项</h3>
    <ul>
      <li>压缩会移除所有 HTML 注释。</li>
      <li>标签间的空白被折叠，但 <code>&lt;pre&gt;</code>、<code>&lt;textarea&gt;</code> 标签内的空白属于内容，建议压缩前单独检查。</li>
      <li>内联 <code>&lt;script&gt;</code>、<code>&lt;style&gt;</code> 内容不做压缩，避免破坏语法。</li>
    </ul>
  </div>
</ToolLayout>
