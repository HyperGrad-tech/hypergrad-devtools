---
import ToolLayout from '../layouts/ToolLayout.astro';
import CronParser from '../components/tools/CronParser.tsx';
---
<ToolLayout slug="cron">
  <CronParser client:load />
  <div slot="seo-content">
    <h2>Cron 表达式解析工具</h2>
    <p>解析 Cron 表达式，分段展示秒/分/时/日/月/周各字段取值，并生成未来最多 100 次执行时间表。支持 5 段（标准）和 6 段（含秒）格式，兼容 MON-FRI、L（月末）、*、/、-、, 等语法。</p>
    <h3>字段顺序</h3>
    <ul>
      <li><strong>5 段</strong>：分 时 日 月 周（如 <code>0 9 * * MON-FRI</code> = 工作日早 9 点）</li>
      <li><strong>6 段</strong>：秒 分 时 日 月 周（如 <code>30 0 9 * * MON-FRI</code> = 工作日 9 点 0 分 30 秒）</li>
    </ul>
    <h3>特殊字符</h3>
    <ul>
      <li><code>*</code> 任意值；<code>/</code> 步长（如 <code>*/5</code> 每 5 分钟）；<code>-</code> 范围；<code>,</code> 列表</li>
      <li><code>L</code> 最后（如日字段 <code>L</code> = 月末最后一天）</li>
      <li>周字段支持 <code>SUN-SAT</code> 或 <code>0-6</code>（0=周日）</li>
    </ul>
    <h3>常见问题</h3>
    <div class="faq-item"><div class="faq-q">时区按什么算？</div><div class="faq-a">本工具按浏览器本地时区计算执行时间。生产环境中 Linux crontab 用服务器时区，Spring/Quartz 等可配置时区，部署时请确认时区一致。</div></div>
    <div class="faq-item"><div class="faq-q">6 段和 5 段有什么区别？</div><div class="faq-a">5 段最小粒度是分钟，6 段多了秒字段，适合需要秒级调度的场景（如 Quartz）。Linux crontab 只支持 5 段。</div></div>
    <div class="faq-item"><div class="faq-q">为什么我的表达式解析失败？</div><div class="faq-a">常见原因：字段数不是 5 或 6 段；取值越界（如月份写 13）；周字段大小写问题（部分实现要求大写）。请对照字段顺序检查。</div></div>
  </div>
</ToolLayout>
