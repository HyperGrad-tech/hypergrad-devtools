---
import ToolLayout from '../layouts/ToolLayout.astro';
import ColorConverter from '../components/tools/ColorConverter.tsx';
---
<ToolLayout slug="color">
  <ColorConverter client:load />
  <div slot="seo-content">
    <h2>颜色转换工具</h2>
    <p>HEX、RGB、HSL 三种颜色模型互转，含实时色块预览、滑动调色、预设色板、点击复制。调任一模型其余两个同步更新，适合前端配色和 UI 设计。</p>
    <h3>三种模型说明</h3>
    <ul>
      <li><strong>HEX</strong>：十六进制表示，如 <code>#2D5F8A</code>，CSS / 设计稿最常用。</li>
      <li><strong>RGB</strong>：红绿蓝三通道 0-255，如 <code>rgb(45, 95, 138)</code>，屏幕显示基础。</li>
      <li><strong>HSL</strong>：色相 H（0-360）、饱和度 S、亮度 L（百分比），如 <code>hsl(207, 51%, 36%)</code>，调色更直观。</li>
    </ul>
  </div>
</ToolLayout>
